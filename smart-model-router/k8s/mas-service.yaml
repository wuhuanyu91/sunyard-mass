apiVersion: v1
kind: Service
metadata:
  name: mas-svc
  namespace: mas
  labels:
    app: mas
spec:
  selector:
    app: mas
  ports:
    - port: 9090
      targetPort: 9090
      nodePort: 30090
  type: NodePort
