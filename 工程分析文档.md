# Metis-Infer 工程分析文档

> 分析对象：`/Users/yizheliu/working/dev/Metis-Infer`（核心子工程 `smart-model-router`）
> 文档版本：2026-08-27 · 基于 v0.1.0-SNAPSHOT 源码静态分析

---

## 1. 工程概览

### 1.1 工程定位

本工程是**信雅达 MAS（智能模型路由访问平台）的可运行原型**，即 TokenGuard 体系中的智能模型路由网关原型。它对外提供 **OpenAI 兼容 API**，对内通过 **L1-L4 四层调度流水线**实现银行场景下的 Token 成本优化：

- **拦截风险**：黑名单、敏感词、参数合法性
- **节约算力**：精确缓存 + 语义缓存（pgvector）两级命中即短路
- **降低成本**：难度路由（simple/complex 模型分流）+ 意图路由 + 加权负载均衡
- **管控配额**：上下文截断压缩 + Token 配额原子预扣

### 1.2 仓库结构

```
Metis-Infer/
├── 智能模型路由访问平台（MAS）设计方案.md   # 设计基准文档（代码注释中的 §/附录 均指向此文档）
├── mas-design-backup.md                    # 设计方案备份
├── 信雅达Maas解决方案_0730.pdf              # 解决方案材料
├── 智能体推理算力优化产品体系计划.pdf        # 产品体系规划材料
└── smart-model-router/                     # ★ 唯一代码工程（Maven 单模块）
    ├── pom.xml
    ├── docker-compose.yml                  # pgvector PostgreSQL 16 + Ollama
    ├── mock_llm.py                         # 无 GPU 环境的 OpenAI 兼容 Mock 后端
    ├── verify.sh                           # 一键验证入口 → tests/run_cases.py
    ├── tests/                              # 900 条黑盒用例（cases.json + 执行器）
    ├── reports/test-report.md              # 测试报告输出
    └── src/main/java/com/sunyard/llm/mas/  # Java 源码（见 §3.2 分层）
```

工程特点：**单模块 Maven 工程、零前端、无网关进程**（Higress 为生产规划，原型以应用自身监听 9090 端口直连验证）。代码注释与设计方案章节严格对应（§2.2、§5.1-5.4、附录 G.x 等），可直接回溯设计依据。

---

## 2. 技术栈

### 2.1 核心技术栈

| 层次 | 选型 | 版本 | 说明 |
|---|---|---|---|
| 语言/运行时 | Java | 17 | |
| 框架 | Spring Boot（**WebFlux**） | 3.3.5 | 全响应式（Reactor），非流式 `Mono` / 流式 `Flux` 透传，禁止缓冲整段流 |
| ORM | MyBatis-Plus（spring-boot3-starter）+ spring-boot-starter-jdbc | 3.5.7 | 阻塞式 JDBC，经 `ReactiveDbAdapter` 调度到 boundedElastic 池适配响应式（替代 R2DBC 的决策） |
| 数据库 | PostgreSQL | 16（镜像） | 许可证 MIT/BSD 类，满足"零 copyleft"红线 |
| 向量扩展 | pgvector | v0.7.0（ankane/pgvector 镜像） | 语义缓存 ANN 检索，HNSW + cosine；Java 侧以文本 `[..]` + `CAST(:v AS vector)` 交互，**无需 pgvector Java 客户端** |
| 本地缓存 | Caffeine | 随 Boot 管理 | L2.1 精确缓存的进程内一级 |
| 限流 | Bucket4j | 8.10.0 | 令牌桶，per-user QPS（默认 20） |
| Token 计数 | jtokkit | 1.1.0 | 后端未返回 usage 时的近似计数（附录 E.6） |
| 敏感词 | Aho-Corasick（ahocorasick） | 0.6.3 | 多模式匹配，词表 `classpath:sensitive-words.txt` |
| 可观测 | Actuator + Micrometer Prometheus | 随 Boot | `/actuator/health`、`/actuator/prometheus` |
| 后端推理 | Ollama（OpenAI 兼容 `/v1`） | 最新镜像 | 本机 gpt-oss:20b（complex 档）/ qwen2.5:0.5b（simple 档）/ bge-m3（embedding） |
| Mock 后端 | Python http.server（`mock_llm.py`） | 标准库 | 无 GPU/离线环境：确定性伪向量（SHA256 派生 1024 维）+ 固定对话回复 |
| 测试 | JUnit5（单测）+ Python 黑盒套件 | — | 16 个单测类；900 条数据驱动黑盒用例 |

### 2.2 选型红线（金融合规约束）

工程严格执行两条约束（见 pom.xml 注释与设计方案附录 A/B）：

1. **零 AGPL/copyleft 依赖**：无 Redis（已禁用，限流/缓存走 Bucket4j + Caffeine + PG）、无 MySQL GPL 依赖，选 PostgreSQL；
2. **信创栈兼容预留**：纯 JVM + 标准 SQL，鲲鹏 ARM64 可直接跑；数据库层可平移 openGauss/KingbaseES（pgvector 依赖需替换为对应向量能力）。

---

## 3. 技术架构

### 3.1 总体架构

```
        调用方（AgentScope 高码智能体 / BOC 低代码平台 / 旧网关）
                │  OpenAI 兼容协议 + Bearer
                ▼
   ┌────────────────────────────────────────────────────┐
   │  Spring Boot WebFlux 应用  :9090  base-path=/smart-router │
   │  TraceWebFilter（X-Trace-Id 生成，贯穿全链路）              │
   │                                                          │
   │  ┌──────────── RoutingPipeline（§2.2）────────────┐    │
   │  │ L1 规则拦截   鉴权/黑名单/敏感词AC/限流/参数校验   │    │
   │  │      │ fail-open 降级                            │    │
   │  │ L2 多级缓存   Caffeine/PG 精确 → pgvector 语义    │    │
   │  │      │ 命中即短路（跳过 L3/L4）                   │    │
   │  │ L3 意图路由   显式model → 难度评分 → 规则意图 → 兜底│    │
   │  │      │                                          │    │
   │  │ L4 执行管控   上下文截断压缩 + 配额原子预扣         │    │
   │  └──────────────────┬───────────────────────────┘    │
   │                     ▼                                  │
   │  ForwardService（WebClient 转发 + 输出脱敏 + usage 兜底）│
   │      │ 流式逐帧透传，边透传边聚合                       │
   │      ├──异步──▶ 两级缓存写入 + mas_call_log 记录        │
   └──────┼──────────────────────────────────────────────┘
          ▼                                   ▼
  Ollama / Mock 后端                PostgreSQL 16 + pgvector
  (chat/completions, embeddings)     (7 张表，见 §5)
```

### 3.2 代码分层

```
com.sunyard.llm.mas
├── MasApplication            # 启动类
├── web/        # 接入层（6 个 Controller + 1 个 Filter）
│   ├── ChatCompletionController   # POST /v1/chat/completions（JSON/SSE）
│   ├── EmbeddingController        # POST /v1/embeddings（透传，不经流水线）
│   ├── ModelController            # GET  /v1/models（启用中对话模型列表）
│   ├── LegacyCompatController     # POST /ai/gateway/chatModel（旧协议兼容）
│   ├── CacheAdminController       # POST /internal/cache/flush（测试/管理面）
│   └── TraceWebFilter             # @Order(-10)，trace_id 生成
├── pipeline/   # 调度流水线（架构核心）
│   ├── PipelineContext / PipelineContextFactory   # 请求上下文（trace/user/auth/请求树/meta）
│   ├── RoutingPipeline           # 编排器：L1→L2→(L3→L4)
│   └── stage/ L1RuleInterceptStage / L2MultiLevelCacheStage /
│             L3IntentRoutingStage / L4ExecutionControlStage
├── service/    # 领域服务（14 个）
│   ├── ChatService        # 入口编排：流水线 + 缓存直返/回放 + 未命中转发
│   ├── ForwardService     # 上游转发、输出脱敏、usage 兜底、缓存/日志异步写
│   ├── ModelRouter        # 模型注册表：DB 快照 + 30s 定时刷新 + 加权随机选择
│   ├── ExactCacheService / SemanticCacheService   # 两级缓存
│   ├── CacheKeyGenerator  # SHA256(model|messages|temperature|max_tokens|stream)
│   ├── RuleIntentClassifier / RuleDifficultyClassifier / IntentClassifier / DifficultyClassifier（接口）
│   ├── RateLimitService / QuotaService / BlacklistService / SensitiveWordFilter
│   ├── TokenCounter       # jtokkit 近似计数
│   └── CallLogService     # 调用记录异步落库
├── entity/  6 实体  ├── mapper/  6 Mapper（注解 SQL）  ├── config/（MasProperties、WebClient、DataSource）
├── util/ReactiveDbAdapter  # 阻塞 JDBC → boundedElastic Mono 适配器
└── exception/（MasException 统一错误码 + GlobalExceptionHandler）
```

### 3.3 L1-L4 流水线细节

| 层 | 组件与逻辑 | 降级策略 |
|---|---|---|
| **L1 规则拦截** | Bearer 非空校验 → 黑名单（DB 表 ∪ 配置名单）→ 敏感词 AC 匹配（仅 user 消息）→ Bucket4j per-user QPS 限流 → 参数校验（messages 非空、max_tokens∈(0,8192]） | 规则组件自身异常 **fail-open 放行**（仅告警） |
| **L2 多级缓存** | ① 精确缓存：SHA256(model+messages+temperature+max_tokens+stream) 键，Caffeine(内存, TTL 30m, 10w 条) + PG mas_exact_cache 双写；② 语义缓存：取最后一条 user 消息 → embedding（bge-m3）→ pgvector 余弦 Top1，相似度 ≥ 0.95 命中 | embedding/PG 异常 **降级跳过**，直接进 L3 |
| **L3 意图路由** | 优先级：显式 model 字段（未注册→404 model_not_found）→ 难度评分路由（score ≥ 0.5 → complex 档大参数模型，否则 simple 档）→ 关键词规则意图分类 → 默认模型（mas.routing.default-model） | 未命中档位回退意图路由，再回退默认模型 |
| **L4 执行管控** | 请求阶段：上下文截断压缩（保留全部 system + 最近轮次，上限 4096 tokens）→ jtokkit 计数 prompt_tokens → 配额原子预扣（prompt+max_tokens，分钟/日双窗口）。响应阶段（ForwardService）：输出全文 AC 脱敏（非流式替换写回；流式仅告警不阻断） | — |

**模型注册表机制**：`ModelRouter` 启动时从 `mas_model_config` 加载内存快照，`@Scheduled(30s)` 热刷新；同意图（intent_type）多实例按 weight 加权随机选择——这是后续管理面的热加载挂载点。生产规划增强项：Resilience4j 熔断故障转移（原型未实现）。

---

## 4. 数据流向

### 4.1 对话主链路（非流式，未命中缓存）

```
Client ──POST /v1/chat/completions──▶ TraceWebFilter(生成trace_id)
  ──▶ PipelineContextFactory(解析body/userId/Authorization/stream)
  ──▶ L1 检查(拒则抛 MasException → GlobalExceptionHandler 统一错误体)
  ──▶ L2 查缓存(exact→semantic，均未命中)
  ──▶ L3 路由(确定 ModelConfig: model_id/endpoint_url/intent)
  ──▶ L4 压缩+计数+配额预扣(mas_token_quota 原子UPDATE)
  ──▶ ForwardService.forwardNonStream:
        WebClient POST {endpoint}/chat/completions   ← body 中 model 已按
                                                      model-overrides 映射为后端真实名
        响应处理: error 检查 → reasoning 兜底(推理型后端) → 敏感词脱敏写回
                 → usage 兜底(jtokkit) → 附加 x-mas-meta(trace/cache/routed_to/cost)
        异步 fire-and-forget:
          ① writeCaches: exact.put → embed(text) → semantic.put
          ② callLog.logAsync → mas_call_log
  ──▶ Client(JSON, 含 x-mas-meta + X-Trace-Id 响应头)
```

### 4.2 缓存命中链路（短路）

```
L2 命中(exact 或 semantic)
  └▶ RoutingPipeline 直接跳过 L3/L4
  └▶ 非流式: ChatService.buildCachedResponse
             — 重生成 id/created，附加 x-mas-meta，callLog 记 0 token
  └▶ 流式: replayCachedStream
             — 完整 content 按 8 字符/chunk 拆分回放为标准 SSE 帧序列
               (delta 帧 → finish 帧 → x-mas-meta 帧 → [DONE])
```

### 4.3 流式链路（未命中）

```
ForwardService.forwardStream:
  WebClient bodyToFlux 逐帧透传("data: {...}\n\n"，禁止缓冲)
  ├─ 每帧 doOnNext: 解析 delta.content / delta.reasoning 分别聚合（reasoning
  │  仅作为推理型后端 content 为空时的兜底来源）
  └─ 完成时 concatWith: 聚合全文 → finalizeStream(脱敏告警/重建响应/写缓存/记日志)
     → 追加 x-mas-meta 帧 + [DONE]
  中途断流: onErrorResume 追加 finish_reason=error 帧后正常结束（G.5.4）
```

### 4.4 辅助链路

- **Embedding 透传**：`POST /v1/embeddings` 按 model 查注册表直转对应 endpoint `/embeddings`（不进流水线，无缓存/计费）。
- **语义缓存 embedding 子链路**：`SemanticCacheService.embed()` 也是经注册表寻址 `bge-m3` 的 endpoint，走同一个 WebClient，向量格式化为 pgvector 文本 `[v1,v2,...]`。
- **旧协议兼容**：`POST /ai/gateway/chatModel` 请求映射（modelId→model、maxTokens→max_tokens）→ 复用完整流水线（统一非流式）→ 响应包裹 `{code:0, message:"success", data:{content, requestId, modelId, usage}}`。
- **模型配置热链路**：DB → ModelRouter 内存快照（30s），L3 路由与 embedding 寻址均读内存，DB 故障时沿用旧快照。

### 4.5 异步与线程模型

```
Netty 事件循环（WebFlux 请求线程，绝不阻塞）
   ├── 缓存/调用日志写入 → ReactiveDbAdapter → Schedulers.boundedElastic()
   ├── HikariCP（max 20 / min idle 5）承接 MyBatis 阻塞 JDBC
   └── @Scheduled: ModelRouter 30s 刷新 / SemanticCache 10min 过期清理
```

---

## 5. 数据模型（PostgreSQL，7 张表）

| 表 | 用途 | 关键字段/索引 |
|---|---|---|
| `mas_model_config` | 模型注册表（路由寻址源） | UNIQUE(model_id)；intent_type 分档；weight/status |
| `mas_call_log` | 全量调用记录（审计+计量） | idx(created_at)；trace_id/cache_hit/cache_level/routed_to/prompt·completion·total_tokens/pipeline_cost_ms |
| `mas_token_quota` | 配额账本（分钟/日窗口） | UNIQUE(quota_type,quota_key,period)；token_used 原子累加 |
| `mas_exact_cache` | L2.1 精确缓存（持久层） | PK cache_key=SHA256(...); idx(expire_at) |
| `mas_semantic_cache` | L2.2 语义缓存 | embedding vector(1024)；**HNSW + cosine**（选 HNSW 因 ivfflat 空表建索引失败） |
| `mas_blacklist` | DB 黑名单（与配置名单取并集） | UNIQUE(subject_type,subject_key)；expire_at |

种子数据（data.sql，幂等）：`qwen-72b`(complex 档→实际 gpt-oss:20b)、`qwen-lite`(simple 档→qwen2.5:0.5b)、`bge-m3`(embedding)——均指向 `http://localhost:11434/v1`。

`model-overrides` 机制说明：路由层 model_id 与后端真实模型名解耦，生产环境后端模型名一致时应留空——这是**多后端异构命名**的适配点。

---

## 6. 接口清单

| 方法 | 路径（前缀 /smart-router） | 功能 |
|---|---|---|
| POST | `/v1/chat/completions` | OpenAI 兼容对话（JSON / SSE），响应附加 `x-mas-meta` |
| POST | `/v1/embeddings` | Embedding 透传 |
| GET | `/v1/models` | 启用中对话模型列表 |
| POST | `/ai/gateway/chatModel` | 旧协议兼容（BOC 低代码平台等） |
| POST | `/internal/cache/flush` | 清空两级缓存（测试/管理面预览） |
| GET | `/actuator/health` `/actuator/prometheus` | 健康检查 / 指标暴露 |

`x-mas-meta` 扩展字段：trace_id、cache_hit、cache_level、intent、difficulty、routed_to、pipeline_cost_ms、total_cost_ms——所有验证断言均围绕该结构。

---

## 7. 质量与验证体系

1. **单元测试**（16 个测试类，surefire）：覆盖流水线上下文工厂、缓存键生成、加权路由分布、限流边界、意图/难度分类器、敏感词过滤、Token 计数、旧协议映射等，含大量 Boundary 测试。
2. **黑盒测试**：`verify.sh → tests/run_cases.py`，**900 条数据驱动用例**（`tests/cases.json`，`gen_cases.py` 生成），逐条断言路由明细并输出 `reports/test-report.md`。曾修复缓存键缺陷：**stream 参数未入键导致流式/非流式响应混淆**（36 例失败，修复后键含 stream）。
3. **Mock 后端**：`mock_llm.py` 提供 SHA256 派生的确定性伪向量（1024 维、同文本同向量），保证无 GPU 环境语义缓存链路可测；阈值行为以真实 bge-m3 为准（设计方案已声明此限制）。
4. **运行方式**：`docker compose up -d`（pgvector + ollama）→ `mvn spring-boot:run`（schema.sql/data.sql 自动执行）→ `./verify.sh`。无 GPU 时以 `python3 mock_llm.py` 替代 Ollama 监听 11434。

---

## 8. 设计亮点与已知限制

### 亮点
- **响应式全链路**：流式逐帧透传零缓冲，Netty 事件循环经 boundedElastic 与阻塞 JDBC 隔离，架构上具备高并发承载基础。
- **降级策略成体系**：L1 规则 fail-open、L2 缓存跳过、缓存写失败仅告警、流式断流补 error 帧——可用性优先于功能完备，符合网关定位。
- **代码-设计强对应**：注释锚定设计文档章节/附录，900 条黑盒用例数据驱动，可直接交给 AI 编程助手继续演进。
- **合规红线落地**：依赖清单可证明零 AGPL/copyleft（PostgreSQL/MIT 系、Caffeine/Bucket4j/Apache2.0、jtokkit/MIT、ahocorasick/Apache2.0）。

### 已知限制与后续项
| 项 | 现状 | 影响 |
|---|---|---|
| 熔断/故障转移 | 未实现（注释标注为生产增强项，规划 Resilience4j） | 上游实例故障时无自动切换 |
| 鉴权 | 仅校验 Bearer 非空，无签名/鉴权体系 | 生产需对接统一认证 |
| 流式输出审核 | 已输出内容不阻断仅告警 | 敏感内容可能已送达客户端（设计取舍 G.7） |
| 语义缓存阈值 | mock 伪向量下仅验证链路连通 | 相似度阈值行为需真实 bge-m3 校准 |
| 单实例假设 | 配额/限流均为单进程内存或单库状态 | 多副本部署需分布式化（注意：Redis 已被红线禁用，需 PG 或其他信创兼容方案） |
| Higress | 生产架构中为主网关，原型未接入 | 原型直连 9090，路由/插件能力待网关层承接 |
| 旧协议映射 | [待确认] 项按默认假设实现 | 需对照 ChatModelInter 实际报文校准 |
